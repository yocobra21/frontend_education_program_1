import slickCarousel from 'slick-carousel'

$('.room-preview__slider').slick({
    dots: true,
})
$('.room-preview__slider').find('.slick-arrow').text('')
$('.room-preview__slider').find('.slick-dots').find('li').text('')

